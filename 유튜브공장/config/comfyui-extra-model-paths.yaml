youtube_factory:
  base_path: ${FACTORY_ROOT}/.runtime/models/comfyui
  is_default: true
  checkpoints: checkpoints
  diffusion_models: |
    diffusion_models
    unet
  text_encoders: |
    text_encoders
    clip
  clip_vision: clip_vision
  vae: vae
  loras: loras
  controlnet: controlnet
  upscale_models: upscale_models
  audio_encoders: audio_encoders
  custom_nodes: ${FACTORY_ROOT}/.runtime/comfyui/custom_nodes
